# Enable build scripts for required dependencies
enable-pre-post-scripts=true

# Allow specific packages to run build scripts
# These are safe dependencies commonly used in Next.js projects
@tailwindcss:oxide=allow
sharp=allow
unrs-resolver=allow
